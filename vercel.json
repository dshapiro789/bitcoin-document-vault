{
    "version": 2,
    "framework": "nextjs",
    "buildCommand": "cd frontend && npm install && npm install tailwindcss postcss autoprefixer && npm run build",
    "outputDirectory": "frontend/.next",
    "builds": [
      {
        "src": "frontend/package.json",
        "use": "@vercel/next"
      }
    ]
  }